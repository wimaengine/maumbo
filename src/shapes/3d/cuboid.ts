export class Cuboid {
  halfWidth
  halfHeight
  halfLength

  constructor(halfWidth: number, halfHeight: number, halfLength: number) {
    this.halfWidth = halfWidth
    this.halfHeight = halfHeight
    this.halfLength = halfLength
  }
}
